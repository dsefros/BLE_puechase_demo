# Limitations

- No permission UX / Bluetooth enable UX in SDK.
- No scanning lifecycle ownership in SDK.
- No UI, payment submission, backend, polling.
- No duplicate/expiry rejection policy in current implementation (duplicate/expired enum reasons are reserved).
